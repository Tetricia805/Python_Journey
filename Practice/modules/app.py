# find the maximum number in the list using a function

import utils

# or from utils import find_maximum
numbers = [10, 70, 50, 5, 1, 0]

max_num = utils.find_maximum(numbers)

print(f"The maximum number in the list is {max_num}")